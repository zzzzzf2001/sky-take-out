<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.EmployeeMapper">

    <insert id="insert">
    insert into sky_take_out.employee(username,name,phone,sex,id_number,password,status,create_time,update_time,create_user,update_user)
    value (#{employee.username} ,#{employee.name},#{employee.phone},#{employee.sex},#{employee.idNumber}
            ,#{employee.password},#{employee.status},#{employee.createTime},
            #{employee.updateTime},#{employee.createUser},#{employee.updateUser})
    </insert>
</mapper>
